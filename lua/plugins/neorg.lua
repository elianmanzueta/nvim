return {
    "nvim-neorg/neorg",
    lazy = false, -- Disable lazy loading as some `lazy.nvim` distributions set `lazy = true` by default
    version = "9.1", -- Pin Neorg to the latest stable release
    config = true,
}
